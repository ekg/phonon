# Simple filtered drum beat
# "bd*4 cp" style beat with DSP filtering

# Create the kick drum (4 on the floor)
~bd: impulse 4 >> mul 60 >> lpf 100 0.9 >> mul 2

# Create the clap (on the backbeat)
~cp_trig: impulse 2 >> delay 0.25 0.0
~cp: ~cp_trig >> mul 40 >> noise >> env 0.001 0.02 0.0 0.03 >> hpf 1500 0.8

# Mix them
~drums: ~bd + ~cp

# Add a filter sweep
~lfo: sin 0.25 >> mul 1500 >> add 2000
out: ~drums >> lpf ~lfo 0.6